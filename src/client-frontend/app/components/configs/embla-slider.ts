import { EmblaOptionsType } from 'embla-carousel';

const options: EmblaOptionsType = {
  loop: true,
  align: "start",
  slidesToScroll: 1,
  // perPage: 3,
  containScroll: "trimSnaps",// Для контролю прокручування

  // align: "start",
  // loop: true,
  // dragFree: false,
  // containScroll: "trimSnaps",
  // slidesToScroll: 1,
};

export default options;

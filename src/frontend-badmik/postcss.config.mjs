// const config = {
//   plugins: ["@tailwindcss/postcss"],
// };

// export default config;

const config = {
  plugins: {
    "@tailwindcss/postcss": {}, // це обробляє @theme, @config, @keyframes тощо
  },
};

export default config;
